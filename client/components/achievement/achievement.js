'use strict';

/**
 * Removes server error when user updates input
 */

angular.module('yoprojApp')
  .directive('myAchmievments', function() {
    return {
      template: 'Name: {{role.title}}'
    };
  });
